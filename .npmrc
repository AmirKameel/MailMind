# Next 15 + React 19 RC: some peers (e.g. @tanstack/react-query) request stable ^19 while we pin the
# exact RC Next ships with. Overrides in package.json align the tree; legacy-peer-deps avoids
# npm 7+ eresolve failures on Vercel/CI when a nested package still disagrees.
legacy-peer-deps=true
